#!/bin/bash
# This is a script to install the shit I generally need on a mac
# after installing port, of course
sudo port install utimer
sudo port install cdargs
sudo port install ImageMagick
sudo port install aspell
sudo port install gnupg